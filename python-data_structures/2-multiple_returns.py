def multiple_returns(sentence):
    if sentence == "":
        return None
    else:
        sentence = ''
        for i in sentence:
            length = len(sentence)
            first = sentence([0][0])
    print("Length: {:d} - First character: {}".format(length, first))
        